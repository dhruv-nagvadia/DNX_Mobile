import { StyleSheet } from 'react-native';
import { Color } from '@/utils/Theme';

export const styles = StyleSheet.create({
  /** Ink placeholder shown while the onboarding flag is read. */
  bootPlaceholder: {
    flex: 1,
    backgroundColor: Color.ink,
  },
});
