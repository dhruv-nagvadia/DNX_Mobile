export { AuthHero } from './AuthHero';
// Exported so other dark screens (onboarding) can reuse the same aurora.
export { Glow } from './Glow';
export type { AuthHeroProps, GlowProps } from './types';
