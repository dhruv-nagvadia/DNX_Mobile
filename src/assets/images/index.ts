// Barrel for image assets. Import from here so paths stay consistent:
//   import { Images } from '@/assets/images';
import glow from './glow.png';

/** glow: white radial gradient (alpha falloff). Recolor it with `tintColor`. */
export const Images = { glow };
